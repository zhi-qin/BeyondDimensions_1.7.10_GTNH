
name: Build and test

on:
  pull_request:
    branches: [ master, main, master_mi ]
  push:
    branches: [ master, main, master_mi ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
